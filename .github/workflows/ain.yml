name: prince-mdxi
services:
  - name: prince-mdxi-service
    type: web
    ports:on
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
